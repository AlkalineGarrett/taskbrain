# React hooks (web)

The hook layer wraps the editor (`useEditor`) and the save coordinator (`useSaveCoordinator`) so React components can drive them without seeing the underlying state objects.

## Cross-platform principles (load these too)

@docs/live-cross-platform-sync.md

## Editor patterns also relevant here

@web/src/editor/CLAUDE.md
